namespace Tools.OrTools
{
    
    public class ObjectiveBuilder<TModel, TSolution> 
        where TModel: class 
        where TSolution: class
    {
        private readonly TModel model;
    }
}